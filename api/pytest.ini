[pytest]
# The integration suite needs a SQL Server and the real database driver, while the unit
# tests replace that driver with a fake for the whole process, so they run separately:
# `pytest` for the unit tests, `pytest tests_integration` for the integration tests.
testpaths = tests
